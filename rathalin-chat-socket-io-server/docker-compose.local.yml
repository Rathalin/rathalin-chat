services:
  rathalin-chat-server:
    build: .
    restart: always
    networks:
      - web-net
    environment:
      - SOCKET_IO_PORT=${SOCKET_IO_PORT}
      - CORS_PORT=${CORS_PORT}
networks:
  web-net:
    external: true
    